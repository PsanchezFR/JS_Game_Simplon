/* Structure for a future camera. For now it's not need so it's empty !*/

define(['Jquery', 'player',], function(jquery, player){
	 class _camera {

		constructor() {
			
		}

		trackPlayer(player) {

		}

	}
	const camera = new _camera();	// creating the only one camera we need
	return camera;
});